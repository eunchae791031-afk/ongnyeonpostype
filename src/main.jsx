import React from "react";
import ReactDOM from "react-dom/client";
import PostypeArchive from "./PostypeArchive";
import "./style.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PostypeArchive />
  </React.StrictMode>
);
